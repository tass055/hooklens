# Secrets — never bake into image (SOC2)
.env
*.env

# Python artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/

# Runtime data — mounted as volumes, not baked in
uploads/
outputs/
*.docx

# Dev tooling
.pytest_cache/
*.egg-info/
